/** @type {import('next').NextConfig} */
const nextConfig = {
  // Otras configuraciones
  staticPageGenerationTimeout: 180, // Aumentar el tiempo de espera en segundos
};

module.exports = nextConfig;